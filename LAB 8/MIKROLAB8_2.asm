STAK    SEGMENT PARA STACK 'STACK'
        DW 20 DUP(?)
STAK    ENDS

DATA    SEGMENT PARA 'DATA'

		SINUS DB 159, 189, 215, 235, 249, 255, 253, 243, 226, 202, 174, 143, 112, 81, 53, 29, 12, 2, 0, 6, 20, 40, 66, 96, 128

DATA    ENDS

CODE    SEGMENT PARA 'CODE'
        ASSUME CS:CODE, DS:DATA, SS:STAK
START PROC
	MOV AX, DATA
	MOV DS, AX
	

	MOV DX, 62H  
	MOV AL, 00H
	XOR SI, SI

ENDLESS:	
	OUT DX, AL
	CALL DELAY
	MOV AL, SINUS[SI]
	INC SI
	CMP SI, 25
        JNZ ENDLESS
	XOR SI, SI
	JMP ENDLESS
	
	RET
START ENDP
	
DELAY PROC NEAR
	PUSH CX
	MOV CX, 0050H ;
	L1:
	LOOP L1
	POP CX
	RET
DELAY ENDP	
	
CODE    ENDS
        END START